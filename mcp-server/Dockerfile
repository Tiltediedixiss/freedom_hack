FROM python:3.12-slim
WORKDIR /app
# Placeholder — will be implemented in T13
RUN echo "MCP server placeholder"
CMD ["python", "-c", "import http.server; http.server.HTTPServer(('0.0.0.0', 9000), http.server.SimpleHTTPRequestHandler).serve_forever()"]
EXPOSE 9000
