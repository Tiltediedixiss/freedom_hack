FROM node:20-alpine
WORKDIR /app
# Placeholder — will be implemented in T11-T12
RUN echo '{"name":"fire-frontend","version":"0.1.0"}' > package.json
CMD ["sh", "-c", "echo 'Frontend placeholder running on port 3000' && npx -y serve -s . -l 3000"]
EXPOSE 3000
